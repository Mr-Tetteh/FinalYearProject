{
  "scripts": {
    "install": "cd Client && npm install",
    "build": "cd Client && npm run build",
    "frontend": "cd Client && npm run preview",
    "backend": "cd Server && php artisan serve",
    "start": "npm-run-all --parallel frontend backend"
  },
  "dependencies": {
    "fetch": "^1.1.0",
    "npm-run-all": "^4.1.5"
  }
}
